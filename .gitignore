# Material del libro: pantallazos Y texto copiado en .md
# (copyright — NUNCA subir, el repo es público). Ignora la carpeta
# completa, así que cubre cualquier formato que se deje adentro.
paginas/

# Compilados de Java (el código fuente .java SÍ se sube)
*.class

# Basura del sistema
.DS_Store
Thumbs.db
.atl/

# Configuracion local de Claude Code (no viaja entre PCs)
.claude/settings.local.json
.codegraph/
