/*
 * EJERCICIO #05 — clase TESTER (TestDrive) de Robot (Sesión #25)
 * Head First Java, pág. 164-169
 *
 * Esta es la clase "TestDrive": la que tiene el main() y prueba tu
 * clase Robot (resolvé primero Robot.java en esta misma carpeta).
 *
 * Acá tenés que, dentro de main():
 * 1. Crear un objeto Robot nuevo usando el operador `new`, guardado en
 *    una variable (ej. r).
 * 2. Usar el operador punto (.) para ponerle un nombre y una batería
 *    (ej. 80) a ese objeto.
 * 3. Usar el operador punto para llamar a saludar() y a cargar() sobre
 *    ese mismo objeto.
 *
 * Si te trabás: revisá la Sesión #25 de GUIA-JAVA.md (ejemplo
 * Dog d = new Dog(); d.size = 40; d.bark();).
 *
 * Recordá que la clase pública debe llamarse igual que el archivo:
 * RobotTestDrive. Escribí tu código DEBAJO de este bloque, todo a mano.
 *
 * PROMPT DE ENTREGA (copiá y pegá esto cuando termines los DOS
 * archivos, Robot.java y RobotTestDrive.java):
 * -------------------------------------------------------
 * Hice el ejercicio #05 (Robot + RobotTestDrive), está en
 * ejercicios/ej05-robot/. Compiló: [sí/no]. Al ejecutarlo:
 * [pego abajo la salida o el error]. Revisalo línea por línea: qué
 * está bien, qué está mal y POR QUÉ, y actualizá su estado y tu
 * corrección en EJERCICIOS.md. Si quedó completado, agendá sus
 * repasos.
 */

public class RobotTestDrive {
    public static void main(String[] args) {
        Robot r = new Robot();
    }
}
