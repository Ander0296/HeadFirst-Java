/*
 * EJERCICIO #06 — Code Magnets: "DrumKit" (pág. 186, Sesión #29)
 * Head First Java
 *
 * Enunciado: un programa completo está pegado con imanes en una
 * heladera, pero alguien lo desarmó y los pedazos quedaron sueltos y
 * desordenados. Además, se cayeron al piso algunas llaves { } de
 * cierre — podés (tenés que) agregar todas las que hagan falta, no
 * vienen todas en los imanes. Reordená los imanes (sin cambiar lo que
 * dice cada uno) para armar DOS clases que compilen y que, al correr
 * `java DrumKitTestDrive`, impriman EXACTAMENTE:
 *
 *     bang bang ba-bang
 *     ding ding da-ding
 *
 * Los imanes disponibles (desordenados, sin llaves de más):
 *
 *   d.playSnare();
 *   DrumKit d = new DrumKit();
 *   boolean topHat = true;
 *   boolean snare = true;
 *   void playSnare() {
 *     System.out.println("bang bang ba-bang");
 *   }
 *   public static void main(String [] args) {
 *   if (d.snare == true) {
 *     d.playSnare();
 *   }
 *   d.snare = false;
 *   class DrumKitTestDrive {
 *   d.playTopHat();
 *   class DrumKit {
 *   void playTopHat () {
 *     System.out.println("ding ding da-ding");
 *   }
 *
 * Este ejercicio tiene DOS archivos — este es la clase DrumKit (el
 * objeto real). Cuando termines este, pasá a DrumKitTestDrive.java
 * (misma carpeta). El prompt de entrega está en ese segundo archivo.
 *
 * Si te trabás: revisá la Sesión #12/#13 (if, boolean) y la Sesión
 * #25 (new, operador punto) de GUIA-JAVA.md.
 *
 * Recordá que la clase pública debe llamarse igual que el archivo:
 * DrumKit. Escribí tu código DEBAJO de este bloque, todo a mano.
 */
